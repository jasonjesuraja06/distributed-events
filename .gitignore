bin/
.DS_Store
.idea/
.vscode/

# bench/reports holds the evidence behind the README table: JSON summaries, the
# driver logs they were derived from, and the docker stats samples the cost
# model reads. All of it is committed so the numbers can be checked.

# Terraform state (private)
deploy/terraform/.terraform/
deploy/terraform/*.tfstate
deploy/terraform/*.tfstate.*
deploy/terraform/.terraform.lock.hcl
